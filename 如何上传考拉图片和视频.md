# 🐨 如何上传考拉图片和视频 - 超详细指南

## 📍 第一步：找到 public 文件夹

### 方法 1：使用 Finder（Mac）或文件资源管理器（Windows）

1. **打开项目文件夹**
   ```
   你的项目完整路径是：
   /Users/hjstudio/Kiro_DoubaoEnglishpal/english-conversation-trainer
   ```

2. **在 Finder 中打开**
   - 按 `Cmd + Shift + G`（Mac）
   - 粘贴路径：`/Users/hjstudio/Kiro_DoubaoEnglishpal/english-conversation-trainer`
   - 按回车

3. **找到 public 文件夹**
   ```
   english-conversation-trainer/
   ├── node_modules/
   ├── public/          ← 就是这个文件夹！
   ├── src/
   ├── package.json
   └── ...
   ```

### 方法 2：使用终端/命令行

```bash
# 进入项目目录
cd /Users/hjstudio/Kiro_DoubaoEnglishpal/english-conversation-trainer

# 进入 public 文件夹
cd public

# 查看文件夹内容
ls -la
```

### 方法 3：使用 VS Code

1. 在 VS Code 中打开项目
2. 在左侧文件树中找到 `public` 文件夹
3. 右键点击 `public` → "在 Finder 中显示"（Mac）或 "在资源管理器中显示"（Windows）

## 📁 public 文件夹当前内容

```
public/
├── favicon.ico
├── index.html
├── koala-placeholder.svg    ← 占位图
├── logo192.png
├── logo512.png
├── manifest.json
├── README_KOALA.md          ← 图片说明
├── README_VIDEO.md          ← 视频说明
└── robots.txt
```

## 🖼️ 上传考拉图片

### 步骤：

1. **准备图片**
   - 找一张可爱的考拉图片
   - 格式：PNG 或 JPG
   - 推荐尺寸：500x500 像素或更大

2. **重命名图片**
   ```
   将图片重命名为：koala.png
   ```

3. **复制到 public 文件夹**
   
   **方法 A - 拖拽**：
   - 打开 Finder，找到 public 文件夹
   - 将 `koala.png` 拖进去

   **方法 B - 命令行**：
   ```bash
   # 假设你的图片在桌面
   cp ~/Desktop/koala.png /Users/hjstudio/Kiro_DoubaoEnglishpal/english-conversation-trainer/public/
   ```

   **方法 C - VS Code**：
   - 在 VS Code 中右键点击 `public` 文件夹
   - 选择 "在 Finder 中显示"
   - 将图片拖进去

4. **验证**
   ```bash
   # 检查文件是否存在
   ls -la /Users/hjstudio/Kiro_DoubaoEnglishpal/english-conversation-trainer/public/koala.png
   ```

5. **刷新浏览器**
   - 按 `Cmd + R`（Mac）或 `Ctrl + R`（Windows）

## 🎬 上传考拉视频

### 步骤：

1. **准备视频**
   - 格式：MP4（推荐）
   - 时长：3-10 秒
   - 大小：< 5MB

2. **重命名视频**
   
   **单个视频**：
   ```
   koala.mp4
   ```

   **多个状态视频**（可选）：
   ```
   koala.mp4              - 默认待机
   koala-listening.mp4    - 录音时
   koala-thinking.mp4     - 思考时
   koala-speaking.mp4     - 说话时
   ```

3. **复制到 public 文件夹**
   
   **方法 A - 拖拽**：
   - 打开 Finder，找到 public 文件夹
   - 将视频文件拖进去

   **方法 B - 命令行**：
   ```bash
   # 假设视频在桌面
   cp ~/Desktop/koala.mp4 /Users/hjstudio/Kiro_DoubaoEnglishpal/english-conversation-trainer/public/
   ```

4. **验证**
   ```bash
   # 检查文件是否存在
   ls -la /Users/hjstudio/Kiro_DoubaoEnglishpal/english-conversation-trainer/public/koala.mp4
   ```

5. **测试视频**
   - 在浏览器中访问：`http://localhost:3000/koala.mp4`
   - 如果能看到视频，说明上传成功！

6. **刷新应用**
   - 按 `Cmd + R`（Mac）或 `Ctrl + R`（Windows）

## 📊 最终文件结构

上传完成后，public 文件夹应该是这样的：

```
public/
├── favicon.ico
├── index.html
├── koala.png                ← 你的考拉图片
├── koala.mp4                ← 你的考拉视频（可选）
├── koala-listening.mp4      ← 录音状态视频（可选）
├── koala-thinking.mp4       ← 思考状态视频（可选）
├── koala-speaking.mp4       ← 说话状态视频（可选）
├── koala-placeholder.svg
├── logo192.png
├── logo512.png
├── manifest.json
├── README_KOALA.md
├── README_VIDEO.md
└── robots.txt
```

## 🧪 测试上传是否成功

### 测试图片
在浏览器中访问：
```
http://localhost:3000/koala.png
```

### 测试视频
在浏览器中访问：
```
http://localhost:3000/koala.mp4
```

### 测试应用
1. 刷新应用页面
2. 查看中间的考拉是否变成了你的图片/视频
3. 查看对话记录中的头像是否也更新了

## 🎯 快速命令（复制粘贴即可）

### 打开 public 文件夹
```bash
open /Users/hjstudio/Kiro_DoubaoEnglishpal/english-conversation-trainer/public
```

### 查看 public 文件夹内容
```bash
ls -la /Users/hjstudio/Kiro_DoubaoEnglishpal/english-conversation-trainer/public
```

### 复制图片到 public（示例）
```bash
# 从桌面复制
cp ~/Desktop/koala.png /Users/hjstudio/Kiro_DoubaoEnglishpal/english-conversation-trainer/public/

# 从下载文件夹复制
cp ~/Downloads/koala.png /Users/hjstudio/Kiro_DoubaoEnglishpal/english-conversation-trainer/public/
```

### 复制视频到 public（示例）
```bash
# 从桌面复制
cp ~/Desktop/koala.mp4 /Users/hjstudio/Kiro_DoubaoEnglishpal/english-conversation-trainer/public/

# 从下载文件夹复制
cp ~/Downloads/koala.mp4 /Users/hjstudio/Kiro_DoubaoEnglishpal/english-conversation-trainer/public/
```

## 🐛 常见问题

### Q1: 找不到 public 文件夹
**A**: 在终端运行：
```bash
open /Users/hjstudio/Kiro_DoubaoEnglishpal/english-conversation-trainer
```
然后在打开的 Finder 窗口中找到 `public` 文件夹

### Q2: 图片/视频上传后不显示
**A**: 
1. 检查文件名是否正确（小写）
2. 检查文件是否真的在 public 文件夹中
3. 硬刷新浏览器：`Cmd + Shift + R`

### Q3: 权限问题
**A**: 如果提示权限不足：
```bash
chmod 644 /Users/hjstudio/Kiro_DoubaoEnglishpal/english-conversation-trainer/public/koala.png
```

### Q4: 文件太大
**A**: 
- 图片：使用 TinyPNG 压缩 https://tinypng.com/
- 视频：使用 HandBrake 压缩 https://handbrake.fr/

## 💡 小技巧

### 快速打开 public 文件夹
创建一个桌面快捷方式：
```bash
# 创建软链接到桌面
ln -s /Users/hjstudio/Kiro_DoubaoEnglishpal/english-conversation-trainer/public ~/Desktop/考拉素材文件夹
```

### 批量处理
如果有多个文件：
```bash
# 进入 public 文件夹
cd /Users/hjstudio/Kiro_DoubaoEnglishpal/english-conversation-trainer/public

# 从其他位置复制所有考拉文件
cp ~/Desktop/koala*.* .
```

## 📞 需要帮助？

如果还是找不到或遇到问题：

1. **在终端运行这个命令**：
   ```bash
   open /Users/hjstudio/Kiro_DoubaoEnglishpal/english-conversation-trainer/public
   ```
   这会直接在 Finder 中打开 public 文件夹！

2. **或者在 VS Code 中**：
   - 打开项目
   - 左侧文件树找到 `public`
   - 右键 → "在 Finder 中显示"

3. **验证路径**：
   ```bash
   # 这个命令会显示 public 文件夹的完整路径
   realpath /Users/hjstudio/Kiro_DoubaoEnglishpal/english-conversation-trainer/public
   ```

## 🎉 完成！

上传完成后：
1. 刷新浏览器
2. 享受你的专属考拉老师！

---

**记住这个路径**：
```
/Users/hjstudio/Kiro_DoubaoEnglishpal/english-conversation-trainer/public
```

把图片或视频放到这里就可以了！🐨✨
