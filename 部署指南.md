# 部署到 Vercel 指南

## 🚀 快速部署步骤

### 方法1: 通过 Vercel Dashboard（推荐）

#### 1. 推送代码到 GitHub

```bash
cd english-conversation-trainer

# 初始化 git（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: English Conversation Trainer with vocabulary cards"

# 创建 GitHub 仓库后，关联远程仓库
git remote add origin https://github.com/你的用户名/english-conversation-trainer.git

# 推送到 GitHub
git push -u origin main
```

#### 2. 在 Vercel 上导入项目

1. 访问 https://vercel.com
2. 点击 "Add New" → "Project"
3. 选择你的 GitHub 仓库 `english-conversation-trainer`
4. 配置项目：
   - **Framework Preset**: Create React App
   - **Root Directory**: `english-conversation-trainer`
   - **Build Command**: `npm run build`
   - **Output Directory**: `build`
   - **Install Command**: `npm install`

5. 点击 "Deploy"

#### 3. 配置自定义域名

部署成功后：

1. 进入项目 Settings → Domains
2. 添加域名：`drama-com.com`
3. 按照提示配置 DNS：
   - 类型：A 记录
   - 名称：@
   - 值：76.76.21.21（Vercel IP）
   
   或者使用 CNAME：
   - 类型：CNAME
   - 名称：@
   - 值：cname.vercel-dns.com

4. 等待 DNS 生效（通常5-30分钟）

#### 4. 配置子路径

在 Vercel 项目设置中：
1. Settings → General → Build & Development Settings
2. 确认 Output Directory 是 `build`
3. 在 Environment Variables 中添加：
   - Name: `PUBLIC_URL`
   - Value: `/english`

#### 5. 重新部署

点击 "Redeploy" 让配置生效

---

### 方法2: 通过 Vercel CLI

```bash
# 安装 Vercel CLI
npm install -g vercel

# 登录
vercel login

# 部署
cd english-conversation-trainer
vercel

# 按照提示操作：
# - Set up and deploy? Yes
# - Which scope? 选择你的账号
# - Link to existing project? No
# - What's your project's name? english-conversation-trainer
# - In which directory is your code located? ./
# - Want to override the settings? No

# 部署到生产环境
vercel --prod
```

---

## 🌐 访问地址

部署成功后，你的应用将可以通过以下地址访问：

- **Vercel 默认域名**: `https://你的项目名.vercel.app/english`
- **自定义域名**: `https://drama-com.com/english`

---

## 🔧 环境变量配置

如果需要配置 API Keys（OpenAI等），在 Vercel 项目设置中添加：

1. Settings → Environment Variables
2. 添加变量：
   - `REACT_APP_OPENAI_API_KEY`
   - `REACT_APP_REPLICATE_API_KEY`
   - 等等...

**注意**：环境变量修改后需要重新部署才能生效。

---

## 📝 DNS 配置详细说明

### 在你的域名服务商（如 Namecheap、GoDaddy、阿里云等）：

#### 选项1: A 记录（推荐）
```
类型: A
主机: @
值: 76.76.21.21
TTL: 自动
```

#### 选项2: CNAME 记录
```
类型: CNAME
主机: @
值: cname.vercel-dns.com
TTL: 自动
```

#### 添加 www 子域名（可选）
```
类型: CNAME
主机: www
值: drama-com.com
TTL: 自动
```

---

## ✅ 验证部署

部署成功后，访问以下地址测试：

1. **主页**: `https://drama-com.com/english`
2. **测试功能**:
   - 点击"开始新对话"
   - 按住麦克风说话
   - 查看词汇高亮
   - 点击单词查看卡片
   - 收藏单词

---

## 🐛 常见问题

### 1. 404 错误
**原因**: 路由配置问题
**解决**: 确保 `vercel.json` 文件存在且配置正确

### 2. 静态资源加载失败
**原因**: PUBLIC_URL 配置错误
**解决**: 检查 `package.json` 中的 `homepage` 字段

### 3. API 调用失败
**原因**: CORS 或 API Key 未配置
**解决**: 
- 检查环境变量
- 确认 API Key 有效
- 检查浏览器控制台错误

### 4. 域名无法访问
**原因**: DNS 未生效
**解决**: 
- 等待 DNS 传播（最多48小时）
- 使用 `nslookup drama-com.com` 检查 DNS
- 清除浏览器缓存

---

## 🔄 自动部署

配置完成后，每次推送到 GitHub 的 `main` 分支，Vercel 会自动：
1. 拉取最新代码
2. 运行构建
3. 部署到生产环境

---

## 📊 监控和分析

在 Vercel Dashboard 可以查看：
- 部署历史
- 访问统计
- 性能指标
- 错误日志

---

## 🎉 完成！

现在你的英语对话训练应用已经上线了！

访问地址：`https://drama-com.com/english`

有任何问题随时查看 Vercel 的部署日志。
